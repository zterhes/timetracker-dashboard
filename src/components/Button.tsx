import React from "react";
import { styled } from "../theme";
import Typography from "./Typography";

export default styled(Typography.withComponent("button"))`
  background-color: unset;
  border: unset;
  cursor: pointer;
`;
